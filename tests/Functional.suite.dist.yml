# Boots a minimal Pimcore kernel (tests/Support/App) against a real database, installs the bundle
# and creates the test classes. Needs PIMCORE_TEST_DB_DSN; the database is dropped and recreated.
suite_namespace: Tsf\GatekeeperBundle\Tests\Functional
modules:
    enabled:
        # must come first: prepares the constants and environment the Pimcore module boots with
        - \Tsf\GatekeeperBundle\Tests\Support\Helper\Gatekeeper
        - \Pimcore\Tests\Support\Helper\Pimcore:
              connect_db: true
